import { CMSPage } from "@/components/cms-page";

export default function PricingES() {
  return (
    <CMSPage 
      slug="pricing-es"
      defaultTitle="Planes de Precios"
      defaultContent="Elige el plan perfecto para tu camino de aprendizaje. Desde Aprendiz hasta Gurú, tenemos opciones para cada nivel de compromiso."
    />
  );
}